namespace GuildManager.Core.Models
{
    /// <summary>
    /// 性別。仕様書 03 §2.4 参照。
    ///
    /// **本作のギルドは女性限定であり、女性以外の冒険者は存在しない**
    /// （→ 01_コンセプト.md：ギルドマスター・アルベールの方針）。
    /// かつては Male / Female の2値を持ち、出現率（MaleGenderChancePercent）を0にすることで
    /// 女性限定を表現していたが、「今後も男性冒険者が登場する予定は無い」という決定を受け、
    /// **設定値の書き換えで男性が復活しうる余地そのものを構造から取り除いた**。
    ///
    /// 列挙型として残してあるのは、立ち絵・氏名生成などが参照する属性としての意味を保つため
    /// （→ Adventurer.Gender）。値が増えることは想定していない。
    /// </summary>
    public enum Gender
    {
        Female,
    }
}
