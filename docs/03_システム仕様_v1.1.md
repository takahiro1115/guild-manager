# 03. システム仕様 v1.1

> v1.0 の詳細仕様を土台に、**矛盾の修正**と**未定義項目の決定**を反映した版。
> 数値そのものはこの文書に書かず、`04_バランス表.xlsx` の該当シート・行を参照する（`→ BAL: シート名/項目`）。
> **決定はすべて「暫定」。** 実際に遊んで調整する。ここでは「破綻しない構造」を固めることが目的。

---

## 0. この版での主な変更（v1.0 からの差分サマリ）

| # | v1.0 の問題 | v1.1 での決定 |
|---|---|---|
| 1 | 致死閾値が青天井（100超で死亡不能）／「低確率ロスト」と実効確率が矛盾／70:30注記が範囲と不整合 | 閾値を `[5,90]` にクランプ。**範囲方式に一本化**し 70:30 注記は廃止（§4.3） |
| 2 | 満足度＝Morale が戦闘士気・忠誠心と命名衝突 | 長期満足度を **`Satisfaction`（満足度）** に改名。`Morale`（戦闘士気）は post-MVP の別概念として予約。忠誠心は当面 Satisfaction に統合（§2.3） |
| 3 | PA が単一スカラーか各ステータス上限か不明 | **ステータス毎に上限 `PA_xxx` を持つ（6値）**。採用等で使う「総合PA」は6値の平均で導出（§2.2） |
| 4 | HP≤0 に至る経路が未接続 | フェーズ2で**個別ダウン判定**を追加。ダウンした者だけフェーズ3へ（§4.2–4.3） |
| 5 | 年齢バケット境界が章間で不整合（27/28） | 年齢帯を**単一の定義表**に統一（§3.0） |
| 6 | 「固定解像度＋拡縮対応」と固定px UI が齟齬 | Godot の stretch 方針を明記し、px は**基準値**と定義（→ 05 技術メモ／§9） |
| 7 | 職業 Scout と顧問 Scout Master が英語名衝突 | 職業 Scout を **`Ranger`（斥候/遊撃）** にリネーム（§2.1） |

未定義だった項目（個人CP式・格付け・経済・列システム 等）は各節で新規定義した。

---

## 1. システム全体構想

- **タイトル:** 冒険者ギルドマネージャー（仮称）
- **エンジン:** Godot **4.2 以降**（C# / .NET 8）※ .NET 8 対応は 4.2 から。対象は PC（Win/Mac/Linux）。C# は Web 書き出し非対応のため Web 版は想定しない（→ 05）
- **解像度:** 論理 1920×1080 を基準。ウィンドウ拡縮は Godot の content scale で対応（→ §9・05）
- **設計方針:** モデル層（Godot 非依存の純 C# ライブラリ）とビュー層（Godot Node/UI）を完全分離。

### 1.2 時間モデル
- 1ターン ＝ 1週。1ヶ月 ＝ 4週。1年 ＝ 12ヶ月 ＝ 48週。
- 目標：20年（960ターン）で最高格付け Sランク到達。達成後エンドレスモードへ。
- 検算：20年 × 48週 = 960ターン。✓
- 期間設計の意図：初期メンバー1世代の稼働年数（稼働の中核は §3.0 の22〜34歳）では
  20年を走り切れず、必ず新人採用と世代交代が要る。「初期メンバーだけでは
  クリアできない」を加齢・強制引退（§3.7）だけで構造的に保証するための年数。

---

## 2. 冒険者データモデル

### 2.1 静的・基本属性

- **ID:** GUID
- **氏名:** 文字列（氏名ジェネレータ。MVP では固定名でよい）
- **年齢:** 整数 15〜40（§3.0 で上限到達時の扱いを定義）
- **職業（JobClass）:**
  - 重戦士（**Warrior**）：耐久・筋力特化、前衛適正高、ヘイト集中
  - 斥候（**Ranger**）：俊敏・索敵特化、前衛/後衛可、不意打ち防止 ※旧 Scout をリネーム
  - 魔導士（**Mage**）：魔力特化、後衛専任、範囲殲滅
  - 神官（**Cleric**）：精神・回復特化、後衛専任、致死回避・負傷軽減
- **性格（Personality）:** 質実剛健 / 合理主義 / 好漢 / 慎重派（効果は §5・§3 に接続。MVP では未使用でよい）

### 2.2 能力値（実効値 1〜100）

STR / AGI / END / MAG / SCT / LDR の6種。各ステータスの役割は v1.0 のとおり。

- **潜在能力 PA：ステータス毎に `PA_STR`…`PA_LDR` の6値を持つ（各 1〜100）。** 実効値は対応する PA を上限に成長する。
- **総合PA** ＝ 6つの PA の平均（採用試験・スカウト評価など「素質の高さ」を1数値で見たい箇所で使用）。
- 生成時の関係：新人はステータス実効値 < PA。序盤は伸びしろがあり、経験（出撃・訓練）で
  実効値がPAに近づいていく。加齢による衰微はPA自体には影響せず、実効値のみを引き下げる
  （§3。v1.1初出の「加齢でPA自体が低下していく」という記述は、衰微処理の実装確定に伴い撤回）。

### 2.3 動的・コンディション属性

- **現在HP / 最大HP：** `最大HP = END×2 + 50`（END 1〜100 → 52〜250）
- **満足度（Satisfaction）:** 0〜100。50未満でサボタージュ傾向、20未満で移籍・退団検討（§5）
  - ※ v1.0 の「Morale」を改名。戦闘中の一時的な士気（Morale）は post-MVP の別フィールドとして予約
- **負傷状態（InjurySeverity）:** なし / 軽傷（全治1〜2週・該当能力-10%）/ 重傷（全治3〜8週・出撃不可・-30%）/ 不可逆障害（完治不能・恒久低下）
- **契約週給（WeeklyWage）:** 毎週引落しの基本給（→ BAL: 経済/週給基準）

### 2.4 採用（新規：新春採用試験）
- タイミング：毎年 新年（第1週）の新春採用試験のみ。年1回。途中採用なし。
  **ただし1年目（ゲーム開始時点の第1週）は対象外。** 初期メンバーが既に配備されているため、
  最初の採用試験は2年目の新年（第49週）から（→ プレイテストで発覚。1年目に既に応募が
  来ると初期配備との整合性が悪いため）。
- 選抜：応募者数名（→ BAL: 採用/応募者数）から、宿舎の空き枠の範囲で
  プレイヤーが選ぶ。採用人数に固定上限なし（枠が許す限り複数可）。
- 契約年齢：15〜18歳。15＝成人（契約下限）。
  - 15歳：実効値がPAから遠い＝伸びしろ最大・長期投資。成長期(15〜21)の1.5倍成長を長く享受。
  - 18歳：実効値がPAに近い＝即戦力・短期。全盛期までの猶予が短い。
  - PA天井は年齢非依存。若い応募者ほど平均PAをやや高く生成してよい（→ BAL: 採用/年齢別PA補正）。
- 雇用枠：現役枠は宿舎Lvに連動。宿舎Lv1で8枠、拡張で解放（§6）。
  顧問は現役枠を占有しない（§7）。
- 契約金：契約時に一時金。総合PA×年齢で変動、即戦力(高PA・年長)ほど高額（→ BAL: 採用/契約金）。
- 維持コスト：契約中は毎週の週給が継続（§2.3・§5.1）。頭数×週給が経営を圧迫し、
  宿舎・医務室・訓練場との投資競合を生む。
- 応募の質：格付けが高いほど有望新人(高PA)の応募率↑（§8.1）＋スカウト顧問+25%（§7）。
- 初期メンバー：採用システム対象外（開始時に別枠配備。将来キャラエディットで選択実装）。

---

## 3. 加齢・成長・衰微モデル

### 3.0 年齢帯の統一定義（v1.0 の境界不整合を解消）

| 年齢帯 | 範囲 | 主な挙動 |
|---|---|---|
| 成長期 | 15〜21 | 成長ロール基礎確率 1.5倍（出撃or訓練場配置が前提。§3.1〜3.4） |
| 全盛期 | 22〜27 | 上下動が最も安定。昇給要求が発生しやすい |
| 円熟期 | 28〜34 | 精神系（LDR/SCT/MAG）維持可。年1回フィジカル衰微 |
| 限界期 | 35〜40 | 衰微が年2回・低下量拡大。自然治癒が2倍遅い。引退傾向 |

> §5.1 の出場機会ペナルティ対象年齢もこの表に合わせ **22〜27歳**に統一（旧「22〜28」を修正）。→ BAL: 満足度/出場機会

### 3.1〜3.4 成長・衰微の数値、および成長トリガー（新規定義）

**成長トリガーは2経路のみ。それ以外（単純待機）では実効値は変化しない。**
EXP・Lvのような中間概念は導入しない。実効値が直接、週ごとの成長ロールで増減する
（v1.1初出のレベルアップ制度は本改訂で廃止。理由は下記）。

#### 経路1：出撃による成長（実戦経験）
- その週クエストに出撃したパーティの全員が対象（成否は問わない）。
- 成長ロール確率 = 年齢帯別基礎確率（成長期は1.5倍、以降の年齢帯で逓減。
  → BAL: 加齢/成長率）× クエスト難易度による係数（→ BAL: 加齢/難易度成長係数）。
- ロール対象ステータスは、職業に応じた簡易重み付けに従って抽選する
  （前衛職はSTR/AGI/END寄り、神官はMAG/LDR寄り、等。→ BAL: 加齢/職業別成長重み。
  §4.2の列システム（前衛/後衛）は未実装のため、現時点では職業のみに基づく重み）。
- 成長ロール成功時、対象ステータスの実効値を+1〜数点で上昇させる
  （→ BAL: 加齢/成長量）。上限は対応する PA_xxx（クランプ、超過しない）。

#### 経路2：訓練場配置による成長（座学・鍛錬）
- 出撃せず、かつ訓練場に配置されている冒険者が対象
  （出撃した週はこちらのロールは行わない＝二重成長なし）。
- 成長ロール確率 = 年齢帯別基礎確率 × 訓練場Lv補正（→ §6・BAL: 施設/訓練場。
  施設Lv本実装は別タスクのため現状は1.0倍固定）。
- ロール対象ステータスは全ステータスから均等抽選。教官（Trainer、§7）が
  配置されていれば、教官の得意ステータスの当選率に加重補正する予定（未実装）。
- 成長ロール成功時の処理は経路1と同じ（+1〜数点、PA_xxxでクランプ）。

##### 経路2：運用ルール確定
- 訓練枠は施設Lv1で1名まで。Lv上昇で枠数を開放する（→ §6・BAL: 施設/訓練場）。
- 配置には毎週の利用費用が発生する（→ BAL: 訓練/週次費用）。宿舎枠のような
  「保有しているだけで無料」ではなく、都度払いのランニングコストとして
  資金処理に組み込む。
- 配置された冒険者は、成長ロール（既存記述のとおり）に加えて、
  §3.5の訓練固有HP処理（微減・回復なし・下限1）を受ける。
- 上記により「訓練場は無制限に使える安全策」ではなく、
  「1名だけ選んで・費用を払って・少しずつHPを削りながら鍛える」
  という毎週の投資判断になる。

#### 経路なし：単純待機
- 出撃もせず訓練場配置もされていない冒険者は、当該週のステータス成長ロールを
  行わない。HPの自然回復（§3.5改）のみ発生する（成長とHP回復は独立した軸）。

#### 成長打ち止めの表現
- 実効値がPA_xxxに到達したステータスはロールしても変化しない
  （自然に成長が止まる。上限到達の明示的な処理は不要、クランプのみで表現できる）。
- 年齢帯が進むほど成長ロール基礎確率は下がる設計とし、育成の主な窓は
  成長期(15〜21)に集中させる（→ BAL: 加齢/年齢帯別基礎確率）。

#### 衰微処理（詳細）
- 対象年齢帯・該当週になったら、STR/AGI/ENDからランダムに1〜2項目を選び、
  実効値を恒久的に低下させる（PAには影響しない。実効値のみ下限0でクランプ）。
- 発生頻度・低下量は年齢帯で変化する：
  - 円熟期(28〜34)：年1回のフィジカル衰微。
  - 限界期(35〜40)：年2回・低下量拡大。
  - 成長期・全盛期は衰微処理の対象外（成長のみ）。
- 発生週・対象ステータス数(1or2)・各ステータスの低下量は
  → BAL: 加齢/衰微量・BAL: 加齢/衰微タイミング に集約。
- 精神系(MAG/SCT/LDR)は本処理の対象外（円熟期の「精神系維持可」の記述どおり、
  限界期でも対象に含めない。将来含める場合は本節の改訂が必要）。
- 稼働年齢の目標曲線：全盛期+円熟期(22〜34)が戦力価値の中核。20〜21で立ち上がり、
  35(限界期)以降で戦力外へ下る。BAL: 加齢 はこの形に収める。
- 育成不足を意図的に残す：1世代の稼働年数では実効値をPAまで伸ばし切れない
  （＝一代では「強すぎるキャラ」を作れない）成長速度に BAL: 加齢/成長率 を設定。
- 世代間増幅で埋める：不足は 教官伝授(§7)＋訓練場(§6)＋格付け上昇に伴う高PA新人の
  流入(§8.1)で世代をまたいで埋める。目安「2世代前後の引継ぎでSランク級に到達し20年目に間に合う」。
  成長率(絞る)と伝授効果(伸ばす)は常に一対で釣り合わせる=本作の中心的バランス方程式。

### 3.5 静養・HP自然回復（疲労Fatigueを廃止し、回復をHPに一本化）
- 疲労度（Fatigue）パラメータは廃止。持久・消耗の軸はHP（END由来）に統合する。
- 出撃した週：戦闘結果に応じて CurrentHP が減少（§4.2 表）。
- 非出撃（待機・静養）の週：CurrentHP を一定量回復（→ BAL: 静養/待機回復）。上限は MaxHP。
- 施設補正：宿舎・医務室 Lv により回復量に倍率（→ §6）。
- 対象範囲：本処理は重傷以外（無傷・軽傷）のHP回復を担う。重傷は §3.6 が別管理
  （InjuryWeeksRemaining をカウントし、全治時に HP を満タン復帰）。
- 「過労」フラグは廃止。連続出撃の抑制は、低HP→個人CP低下（§4.2）による
  戦闘結果の悪化で軟らかく働く（ハードな出撃不可ゲートは重傷のみ）。

### 3.5 静養・HP自然回復（つづき：訓練場配置の扱い）

- 訓練場に配置され、かつ出撃していない週は、§3.5前段のHP自然回復を**適用しない**。
  代わりに以下の訓練固有のHP処理を行う：
  - CurrentHP を一定量減少させる（→ BAL: 訓練/HP消費）。
  - ただし CurrentHP は 1 未満にはならない（下限クランプ）。
  - この減少は負傷（InjurySeverity）を発生させず、致死・不可逆障害の判定
    （§4.3）には一切接続しない。訓練は「回復しない・地味に削れる」だけの
    低リスクな経路として扱う。
- 出撃も訓練場配置もしていない（単純待機の）週のみ、前段のHP自然回復が適用される。
- 3つの状態（出撃／訓練場配置／単純待機）は互いに排他。同じ週に重複しない。

### 3.6 負傷回復処理（v1.1で欠落判明・新規追加）

**発覚の経緯:** Phase 2 のMVPを実際にプレイしたところ、重傷になった冒険者が一度も回復せず、詰み状態のまま出撃させ続けられることが判明した。§2.3で負傷の全治週数（軽傷1〜2週／重傷3〜8週）をデータとして定義していたにもかかわらず、それを消化して治す処理がv1.0・v1.1のどちらにも存在していなかった。

- 負傷した週に、その負傷の全治週数を `InjuryWeeksRemaining` としてランダムに決定し保持する（軽傷1〜2週、重傷3〜8週の範囲で）。
- 毎週の決算処理で、負傷している全冒険者の `InjuryWeeksRemaining` を1減らす。
- 0に達したら `Injury = None` に戻し、`CurrentHP = MaxHP` として全快させる。
- **出撃制限:** `Injury == Severe` の間は編成画面で選択できないようにする（MVPのUIには未実装だったため、Phase 2の修正で追加）。
- 不可逆障害（Permanent）はこの処理の対象外（そもそも完治しない。仕様書 §2.3・§4.3参照）。

### 3.7 年齢上限到達の扱い（旧§3.6を繰り下げ）

- 40歳の年の年末（第48週決算）に**強制引退**。以後は顧問として再任用可能（§7）。
- 限界期は毎年「引退勧告（肩たたき）」を提示でき、受諾率は年齢とともに上昇（→ BAL: 加齢/引退勧告受諾率）。
  ※受諾判定の実装は任意（未実装。40歳到達時の強制引退のみ必須実装とした）。

### 3.7 年齢上限到達の扱い（つづき：実装時点での顧問機能の扱い）

- 現時点（§7顧問制度が未実装の間）は、40歳強制引退した冒険者を
  「引退済み・顧問候補」として保持するのみとし、教官・スカウト・
  参謀としての実際の効果（伝授・応募率補正・事故率抑制）は付与しない。
- §7実装時に、この保持データ（引退済み一覧）から顧問へ再任用できる
  ようにする。データ構造はこの前提で設計する（削除せず保持）。

---

## 4. クエスト・遠征解決エンジン

「次週へ」実行時、派遣中の各パーティに対して以下の順で解決する。

### 4.0 クエストの定義（スケール確定）

各クエストは以下を持つ。**難易度・索敵要求・報酬はランクに紐づく**（→ BAL: クエスト）。

- **ランク:** E / D / C / B / A / S（ギルド格付けと同じ体系）
- **QuestDifficulty:** 1〜100 の整数（ランク帯ごとの範囲は → BAL: クエスト/難易度レンジ）
- **QuestScoutRequirement:** 1〜100（SCT と同スケール。→ BAL: クエスト/索敵要求）
- **報酬（ゴールド）／期限（週）／放置危険度**

### 4.1 フェーズ1：索敵・遭遇判定

```
PartyScout = max(SCT) + 平均SCT×0.3 + 隊長LDR×0.2
ΔS = PartyScout − QuestScoutRequirement
Roll = Random(1,100)
```

- `Roll ≤ clamp(15+ΔS, 1, 99)` → 奇襲成功：パーティ戦闘力 +30%
- 上記超〜`clamp(80+ΔS, 1, 99)` → 通常交戦：補正なし
- それ超 → 不意打ち：敵戦力 +30%、後衛の被弾ウェイト上昇（§4.3）

> v1.1 で **境界を 1〜99 にクランプ**（ΔS が極端でも帯が消えない・逆転しないように）。

### 4.2 フェーズ2：戦闘比率と個別ダウン判定

**個人CP（v1.0 で未定義 → 新規定義）:**

```
個人CP = ( STR×wSTR + AGI×wAGI + END×wEND + MAG×wMAG + LDR×wLDR )
         × 職業配置補正(職業, 配置)
         × (現在HP / 最大HP)
```

- 重み `wXXX` と職業×配置（前衛/後衛）の補正は → BAL: 戦闘/CP重み・配置補正。
- SCT は CP に含めない（索敵フェーズ専任）。これで全ステータスに役割が付く。

```
PartyCP = Σ個人CP × 索敵補正
EnemyCP = QuestDifficulty × 3.0        （→ BAL: 戦闘/敵CP係数）
Ratio   = PartyCP / EnemyCP
```

※ HPの影響は個人CPの ×(現在HP/最大HP) で保持。疲労項は廃止。

**勝敗と基本損耗（HP消費%）:**

| 結果 | 条件 | 任務 | HP消費 | 個別ダウン判定 |
|---|---|---|---|---|
| 完全勝利 | Ratio ≥ 1.8 | 達成 | 5〜15% | 無し |
| 辛勝 | 1.0 ≤ Ratio < 1.8 | 達成 | 20〜45% | 無し |
| 苦戦敗退 | 0.6 ≤ Ratio < 1.0 | 失敗 | 40〜70% | 各員が確率でダウン → §4.3 |
| 戦線崩壊 | Ratio < 0.6 | 壊滅 | — | 各員が高確率でダウン → §4.3 |

- 損耗の方針：campaign長期化(20年)に伴い HP消費%(上表) は重めに置き、
  静養→ローテーションを頻発させてテンポを作る。重くするのは主に勝利行
  (完全勝利・辛勝)であり、敗北率そのものは上げない——ロストの希少さは
  「敗北時のみダウン判定」で担保する既存方針(§4.3)を維持（→ BAL: 戦闘/HP消費）。

**個別ダウン判定（v1.0 の「HP≤0 経路」欠落を解消）:**

- 損耗%を各員の現在HPに適用したうえで、苦戦敗退・戦線崩壊では各員が「ダウン」ロールを行う。ダウン確率は結果ランクで変わる（→ BAL: 戦闘/ダウン確率）。
- **AGI が高い者はダウン確率が下がる**（＝逃走成功の表現。v1.0 の「AGI 逃走成功率」をここで接続）。
- ダウンした者だけがフェーズ3（致死判定）へ進む。ダウンしなければ重傷止まり。

### 4.3 フェーズ3：負傷・致死判定（低確率ロスト仕様）

ダウンした冒険者に対して判定する。

```
SurvivalThreshold = clamp( END + 神官MAG×0.4 + 隊長LDR×0.2 , 5 , 90 )   ← v1.1 でクランプ追加
PermanentBand     = 20   （→ BAL: 戦闘/不可逆帯幅。調整可）
Roll = Random(1,100)
```

- `Roll ≤ SurvivalThreshold` → **生存**：HP1で踏みとどまり重傷（全治3〜6週）
- `SurvivalThreshold < Roll ≤ SurvivalThreshold + PermanentBand` → **不可逆障害**：永続デバフ。引退・顧問化へ誘導
- `Roll > SurvivalThreshold + PermanentBand` → **戦死**：キャラクター完全ロスト

> **修正の要点:** (1) 閾値を最大90にクランプしたので、どんなに硬い前衛でもロストは 0% にならない。(2) 範囲方式に一本化し、v1.0 の「70%/30%の割り当て」注記は廃止（範囲と矛盾していたため）。ロストの希少さは「フェーズ2でそもそもダウンしにくい」ことで担保する設計に変更。

### 4.4 フェーズ4：治安連動処理

- 放置された討伐クエスト1件につき街の **脅威度 +2〜5%**（→ BAL: 治安）。
- クエスト達成で該当地域 **-10〜15%**。
- 脅威度 75% 超で市場素材価格が高騰し、月次助成金が 50% カット。
- **地域モデル:** MVP は**単一の街（脅威度1値）**。複数地域は post-MVP（→ §11 未決事項）。

---

## 5. クラブ運営・人事マネジメント

### 5.1 満足度（Satisfaction）変動ロジック

毎週日曜の決算時に増減（各係数 → BAL: 満足度）。

- 出場機会：22〜27歳が「4週連続で遠征なし」なら毎週 -5
- 賃金妥当性：実力ランクに対し週給が適正値の80%未満なら毎週 -8
- 人間関係：相性「険悪」（→ §5.3）のメンバーと同パーティ出撃で -10
- 勝利・功績：Bランク以上クエスト達成で +10
- 仲間ロストの余波：同パーティの死亡で一律 -30 ＋確率でトラウマ特性付与（→ §5.3）
- 自然回復：酒場 Lv により毎週 +（→ §6）

### 5.2 契約交渉と退団

- 満足度20未満でインボックスに「昇給要求」または「移籍検討」警告。
- 2週以内に昇給（週給 1.5〜2.0倍提示）またはボーナスを行わなければ自発的に契約解除・他都市へ移籍（消滅）。

### 5.3 相性・特性システム（v1.0 で参照のみ → 新規定義）

- **相性値:** 冒険者ペアごとに -100〜+100 を保持。ゲーム開始時に生成（乱数 ＋ 性格相性）。同パーティ出撃の成否で漸増減。閾値「険悪 = -40以下」「盟友 = +40以上」（→ BAL: 相性）。
- **特性（Trait）:** 少数の列挙型から開始（例：豪胆／トラウマ／師匠肌 等）。効果は満足度・成長・戦闘に小補正。**MVP・序盤機能では未実装、後から追加**。

---

## 6. 施設・インフラ拡張（4大施設 Lv1〜5）

宿舎・医務室 / 訓練場・道場 / 作戦資料室 / ギルド酒場。各 Lv の効果・**改築費**は → BAL: 施設。
（v1.0 の Lv1/3/5 の効果値を土台に、Lv2/4 の中間値と全 Lv の費用を BAL 側で補完する。）

- 訓練場・道場の枠数：Lv1＝1名、Lv上昇で開放（具体的な各Lvの枠数・改築費・
  週次利用費用は → BAL: 施設/訓練場）。

---

## 7. 引退とセカンドキャリア（顧問制度）

- 退職金（週給 × 12週）一括支給で顧問として再任用。
- **教官（Trainer）:** 訓練場に配置。訓練中の若手へ、自身の秀でたステータスを伝授（確率・効果 → BAL: 顧問/教官伝授）。
- **スカウト（Scout Master）:** 新春採用試験で総合PA 75以上の有望新人応募率 +25%。
- **作戦参謀（Advisor）:** 作戦資料室に配置。勝率計算精度を上げ事故率を抑制。
- **保有枠との関係（新規決定）:** 顧問は宿舎の保有枠（現役枠）を**占有しない**。顧問は各施設の専用スロット（教官枠 等）にのみ入る。

---

## 8. 勝敗判定条件

### 8.1 ギルド格付け（v1.0 で未定義 → 新規：勝利条件の根幹）

- ギルドは **名声（Reputation）** 整数を持つ。クエスト達成で加算、失敗・ロストで減算（値 → BAL: 格付け/名声増減）。
- 名声の累積閾値でランクが上がる：**G → F → E → D → C → B → A → S**（閾値 → BAL: 格付け/ランク閾値）。
- ランクは「受注可能クエストの最高ランク」と「助成金額」に影響。

### 8.2 勝利条件
- 20年目 第48週（第960ターン）終了時にギルド格付けが Sランクなら勝利。
- クリア後：「名誉ギルドマスター」称号でエンドレス経営モードへ（セーブ継承）。

### 8.3 敗北条件（成立した週の終了時に即時敗北）

- **破産:** 週次決算で所持金マイナスが **4週連続**で解消されない。
- **治安崩壊:** 脅威度が **100%** に到達。

---

## 9. 画面レイアウト（MainDashboard.tscn）

- 常設は2ペイン構造（ヘッダー／中央＝クエスト一覧+編成/ステータス詳細／右＝週報）＋フッタ。
- 新春採用試験（§2.4）は常設ペインを持たない。年1回・新年第1週（2年目以降）にのみ
  モーダルポップアップ（PopupPanel等、同一ウィンドウ内オーバーレイ）として自動表示する。
- 採用処理（採用する/見送るの意思決定）が完了するまで「次週へ進める」をブロックする。
  OSレベルの別ウィンドウ（Godotの`Window`ノード）は使わない
  （マルチウィンドウはプラットフォーム差異が出やすいため）。
- px 値の扱いは既存方針（論理1920×1080基準・content_scale対応）を継続。
- 中央下部の「次週へ進める（Next Week）」ボタンは Space キー連動。

---

## 10. 未使用ステータスの接続状況（設計メモ）

MVP のエンジンで各ステータスがどう効くかの一覧。「飾りになっていないか」の点検用。

| ステータス | 接続先 |
|---|---|
| STR | 個人CP |
| AGI | 個人CP ＋ ダウン確率低減（逃走）§4.2 |
| END | 最大HP・致死回避閾値 §4.3 |
| MAG | 個人CP・神官の致死回避補正 |
| SCT | 索敵フェーズ §4.1（CP には不参加） |
| LDR | 索敵補正・個人CP・致死回避補正 |

> 戦闘中の一時的士気（Morale）・パニック・回避判定は **post-MVP の追加レイヤー**として保留（§11）。

---

## 11. 明示的な未決事項（今は決めない、遊んでから決める）

構造には影響しないが、いずれ決める必要があるもの。**ここに置いておくことで「決め忘れ」を防ぐ。**

- 戦闘中の一時的士気・パニック・回避の独立実装をするか（現状 AGI/LDR は別経路で接続済み）
- 複数地域の治安モデルへ拡張するか
- 特性（Trait）の種類と効果の拡張
- （決定済み・§2.4へ昇格）採用は年1回・新春採用試験のみに確定
- 難易度設定（易しい/普通/難しい）を設けるか
- 氏名ジェネレータの文化圏・語彙

**新規スコープ（今回の実装対象ではないが、忘れないよう記録のみ残す）:**

- ライバルギルドと引き抜き機能（途中採用の唯一の経路）— post-MVP
- 武器・装備の開発／装備システム — 新規スコープ。個人CP式(§4.2)に装備補正項の
  追加が前提。時間経過を要する投資要素として施設・訓練と同系で後日設計 — post-MVP
