# 06. タスクリスト

> 「次に何を作るか」の順序付きリスト。GitHub Issues や Trello に移してもよい。
> **一度に着手するのは1つ。** 前が動いてから次へ。チェックが埋まる感覚が開発を続ける燃料になる。

---

## Phase 0：準備・学習（数日〜）

- [ ] Godot 4.2+（.NET 版）と .NET 8 SDK をインストール
- [ ] Git / GitHub リポジトリを作成し、この docs 一式を最初のコミットとして入れる
- [ ] Godot の C# 公式チュートリアルを1〜2本こなす（捨てる前提の小プロジェクトで Node/Scene/Signal と C# に慣れる）
- [ ] `GuildManager`（Godot）と `GuildManager.Core`（C# クラスライブラリ）の2プロジェクト構成を作り、参照を通す（→ 05 技術メモ §2）
- [ ] `.gitignore` を設定（Godot の `.godot/`、ビルド成果物を除外）

## Phase 1：MVP のコア（データとロジック）

> ゴール：`02_コアループとMVP.md` の「MVP 完成の判定基準」を満たすこと。

- [ ] `Adventurer` モデル（6ステータス・職業・HP・週給。まだ加齢/PA/性格は空でよい）
- [ ] `Quest` モデル（ランク・QuestDifficulty・索敵要求・報酬）
- [ ] `Party` モデル（4スロット。前衛/後衛はまだ無し）
- [ ] `GameState` モデル（所持金・週番号・冒険者/クエスト一覧）— **最初からシリアライズ可能に**
- [ ] 固定データで冒険者4〜6名・クエスト2〜3件を用意
- [ ] `QuestResolver`：フェーズ1（索敵）＋フェーズ2（戦闘比率）＋簡易致死（HP0で重傷のみ）
- [ ] 個人CP 計算式を実装（→ 03 §4.2、重みは仮値で直書き可、後で外部化）
- [ ] 週送りで「週給引落し ＋ 報酬加算」の資金処理
- [ ] （テスト）QuestResolver の勝敗境界をユニットテストで確認

## Phase 2：MVP の画面（最小 UI）

- [ ] 中央ペイン：クエスト一覧＋4スロット編成（ドラッグ配置）
- [ ] 右ペイン：週送り結果のログ表示
- [ ] フッタ：「次週へ」ボタン（Space 連動）
- [ ] ヘッダ：週・所持金の表示
- [ ] **一周まわることを複数週連続で確認 → MVP 完成判定**

## Phase 3：機能追加（1つずつ・動作確認しながら）

> 各項目は独立の Issue にして、着手 → 実装 → 手動確認 → コミットの単位で進める。

- [x] **加齢・成長・衰微モデル（→ 03 §3）2026年9月実装・テスト済み**
  - [x] 成長トリガー：出撃経験（経路1）・訓練場配置（経路2）の週次ロール実装
  - [x] 訓練場配置の最小限フック（施設Lv本実装は別タスク §6。→ 施設Lv投資の実装により訓練場枠数はLv連動接続済み）
  - [x] 訓練場の運用ルール：枠数上限(Lv1=1名)・週次利用費用・
        訓練週のHP微減(下限1・回復なし・致死判定と非接続)
  - [x] PAクランプ処理
  - [x] ~~衰微処理（年齢帯別・低下対象ランダム抽選）~~ **→ 2026年9月、8年稼働モデルへの
        改訂により完全廃止（下記「8年稼働・26歳満期引退モデル」参照）。取り消し線は
        「一度実装したが後に撤去した」ことを明示するため。**
  - [x] レベル/EXP表示の削除（仕様に存在しないため。旧レベルアップ制度は本改訂で廃止）
  - [x] ~~40歳強制引退（顧問機能は§7実装まで保留・フラグとデータのみ保持）~~
        **→ 2026年9月、26歳・8年稼働モデルへ置き換え済み。顧問機能自体も§7として
        本実装済み（下記参照）。**
  - [ ] （任意）引退勧告（肩たたき）の受諾判定
- [x] **8年稼働・26歳満期引退モデルへの全面改訂（衰微の廃止）（→ 03 §3・§11）
      2026年9月実装・テスト済み**
  - [x] 加齢によるステータス低下（衰微）を完全廃止（`ApplyDecline`と対応する
        バランス値 `MatureDecline*`/`LimitDecline*` を削除）
  - [x] 加入年齢を18歳固定に統一（`MinCandidateAge` = `MaxCandidateAge` = 18、
        `YoungestAgePaBonus` = 0）
  - [x] 26歳の年度末（48週目）で満期引退。先に加齢→判定の順序で稼働期間が
        ちょうど8年（384週）になるよう実装
  - [x] `Adventurer.ActiveWeeks`（稼働週数）・`TotalContributionScore`（累積功績）を追加
  - [x] 退職金＝週給×12週＋累積功績×係数（`SeveranceContributionCoefficient`）。
        払いきれない場合も引退は成立させ、名声が低下する
  - [x] `QuestDispatchSystem`によるクエスト解決時の累積功績加算（難易度比例・達成時倍加）
- [x] **負傷回復ロジック（全治週数のカウントダウン）（→ 03 §3.6）※MVPプレイテストで発覚した欠落。優先度高め。2026年9月実装・動作確認済み**
- [x] **採用（新春採用試験）システム（→ 03 §2.4）※宿舎Lv連動（§6）は施設Lv投資の実装により接続済み。格付け連動（§8.1）・スカウト顧問補正（§7）は未実装のため固定値/TODOフックでスタブ。2026年9月実装・テスト済み**
- [x] **満足度と退団（→ 03 §5.1–5.2）2026年9月実装・テスト済み**
  - [x] 満足度変動（出場機会・賃金妥当性・勝利功績・ロスト余波）
        ※ロスト余波はメソッド実装済みだが、§4.3の致死判定が未実装のため現状どこからも呼ばれない
  - [x] 相性による満足度変動 → §5.3.1実装（v1.4改訂）により接続済み
  - [x] 酒場Lvによる自然回復量の連動 → §6実装により接続済み
  - [x] 契約交渉フロー（警告→2週猶予→昇給/ボーナス/契約解除）
- [x] **前衛/後衛の列システムと配置補正（→ 03 §4.2）2026年9月実装・テスト済み**
  - [x] Placement属性の新規実装（永続・職業デフォルト。職業による固定は無く全職業で自由に変更可）
  - [x] 個人CP計算式への接続
  - [x] 不意打ち時の後衛被弾ウェイト接続（§4.1↔§4.3）
- [x] 施設 Lv 投資（4施設）（→ 03 §6・§6.1）2026年9月実装・テスト済み
  - [x] 建設キュー（同時着工1件・工事期間・着工時前払い）
  - [x] 訓練場枠数・宿舎現役枠・医務室回復倍率・酒場満足度回復のLv連動接続
- [x] 複数週クエスト（→ 03 §4.0・§4.0.1）2026年9月実装・テスト済み
  - [x] Questモデルに探索規模(Scale)・拘束週数(DurationWeeks)を追加
  - [x] 派遣中状態の管理（GameStateでの進行中案件保持）
  - [x] 満了週にのみ解決ロジックを実行、成長ロールも満了週の1回のみ
  - [x] 派遣中メンバーをパーティ編成・訓練場配置から除外
  - [x] 呼び戻し（中断）は実装しない
- [x] ギルド格付け・名声（§8.1：昇格・降格・ヒステリシス・名声自然減衰）
  - [x] 昇格・降格ライン別設定（ヒステリシス）
  - [x] 降格処理（週次決算での判定）
  - [x] 名声自然減衰（現ランク相当クエスト未達成が続いた場合）
- [ ] 勝利条件（§8.2：未定・構想段階。最終討伐クエスト案。バランス確定後に設計）
- [x] 不可逆障害・戦死（→ 03 §4.3・§5.3）※コア体験の核 2026年9月実装・テスト済み
  - [x] 特性(Trait)汎用エンジンの新設（効果種別enum・カタログ・重複禁止）
  - [x] 古傷をTraitの最初の実例として実装（StatPercentReduction接続）
  - [x] 致死判定フェーズ3の本実装（生存/古傷/戦死の3分岐、重複時は
        生存(重傷)へ丸める）
  - [x] 戦死記録（週番号・年齢・職業を保持しロースターから除外）
  - [x] 仲間ロストの満足度ペナルティ(-30)との接続
  - [x] 索敵判定補正の実接続（「注意深い」特性、ScoutingModifier、v1.4改訂）
  - [ ] （post-MVP）成長率補正の実接続（GrowthRateModifier）、他特性（頑強・師匠肌等）の追加
- [x] クエスト種別(QuestType)追加（討伐/調査探索/護衛）（→ 03 §4.0）2026年9月実装・テスト済み
- [x] ~~治安・脅威度システム（→ 03 §4.4・§8.3）~~
      **→ 2026年9月、システムごと完全撤去（下記「脅威度・治安システムの残骸整理」）。**
  - [x] ~~討伐クエストの放置・失敗で脅威度上昇、達成で脅威度減少~~
  - [x] ~~脅威度75%超で月次助成金50%カット~~
  - [x] ~~脅威度100%到達で即時敗北（猶予なし、破産条件とは区別）~~
        **→ 2026年9月、敗北条件純化（下記）により撤廃。**
- [x] **敗北条件の純化・ボス配置間隔のCSV外部化（10F刻み）・出撃枠拡張の実装
      （→ 03 §0.3・§4.4・§4.5.1・§8.3）2026年9月実装・テスト済み**
  - [x] 治安崩壊（脅威度100%到達）による即時敗北を撤廃。敗北条件を
        経営破綻（4週連続赤字による資金ショート）のみへ一本化
        （`DefeatSystem`。`DefeatReason.SecurityCollapse`列挙子は旧セーブ
        互換のため残置、新規にこの理由で敗北することはない）
  - [x] 討伐クエストの放置（期限切れ）による脅威度上昇を無効化
        （`SecuritySystem.ApplyAbandonedQuest`は常に0を返す）。
        当時は脅威度自体を月次助成金50%カットの判定材料として存続させたが、
        後に脅威度ごと完全撤去した（→ 下記「脅威度・治安システムの残骸整理」）
  - [x] 階層ボスの配置間隔を5階→10階へ変更し、`dungeon.csv`の
        `BossIntervalFloors`へCSV外部化（1フィールド計10体、全5フィールド
        で計50体、`DungeonBalance.BossIntervalFloors`）
  - [x] 出撃枠拡張トリガーを新設：森（Order=1）の節目ボス撃破のみが対象。
        森10Fボス撃破で2枠（Rank E相当）、森20Fボス撃破で3枠（Rank D相当）
        まで拡張（`DungeonExpeditionSystem.ApplyFieldProgression`→
        `SyncSquadSlotsAndRank`。「古代エルフの多頭通信術式の復元」という
        世界観づけ、→ 01 コンセプト）
- [x] 引退時実効ステータスの参照（生涯ピーク値廃止に伴うスリム化完了）（→ 03 §2.2・§7）2026年9月実装・テスト済み
  - [x] `Adventurer.PeakSTR`〜`PeakLDR`、`AdventurerStatAccessor.GetPeak/SetPeak`、
        `GrowthSystem`のピーク更新処理を撤去。顧問ボーナス（`AdvisorSystem`）と特性伝授ボーナス
        （`TrainingSystem`）は実効値を直接参照。旧セーブの`Peak*`項目はロード時に無視される
- [x] 訓練場を4施設に分割（戦士訓練所/教会/魔法研究所/斥候所）（→ 03 §6）2026年9月実装・テスト済み
- [x] 顧問制度の本実装（→ 03 §7）2026年9月実装・テスト済み
  - [x] 早期引退（任意タイミング）の追加
  - [x] 教官：施設別1名・対象ステータスの実効値に連続比例したボーナス
  - [x] 参謀：7能力の実効値平均に連続比例したボーナス
  - [x] スカウト：LDR・DEXの実効値平均に連続比例したボーナス
- [x] 相性・特性システム（→ 03 §5.3）2026年9月実装・テスト済み
  - [x] 相性(Compatibility)：ペア値・変動(達成/失敗/戦死居合わせ)・険悪判定
  - [x] §5.1相性険悪ペナルティの接続
  - [x] Trait効果種別拡張（SurvivalThresholdModifier・CompatibilityGainMultiplier）
  - [x] トラウマ（戦死現場居合わせで確率付与）実装、§5.1接続
  - [x] 豪胆・注意深い・容姿秀麗（採用時の先天的付与）実装
- [x] **HP自然回復（静養）ロジック（待機週にCurrentHP回復・施設補正）（→ 03 §3.5改）※疲労Fatigue廃止に伴う新規実装。施設Lv補正は施設Lv投資の実装により医務室Lv連動接続済み。2026年9月実装・テスト済み**
- [x] 基礎ステータス名称変更（END→VIT / SCT→DEX / MAG→MND、役割不変）
  - [x] docs/03、Adventurer.cs、QuestResolver.cs、SampleData.cs、UI表示の一括リネーム
  - [x] INT予約フィールドの追加（未接続、将来拡張用）
- [x] DEX・INTを個人CPに追加（→ 03 §4.2、二重役割・活性化）
  - [x] CP計算式にDEX・INT項を追加
  - [x] PA_INT追加、総合PAを7値平均に拡張
  - [x] 成長トリガー対象プールにINT追加（衰微対象には含めない）
  - [x] SampleData.csにINTの暫定初期値を設定
- [x] 施設初期レベルの修正（→ 03 §6）2026年9月実装・テスト済み
  - [x] 訓練4施設・作戦資料室・冒険者支援室をLv0（未建設）スタートに変更
  - [x] 宿舎・医務室・ギルド酒場は既存どおりLv1のまま
  - [x] Lv0の施設は訓練枠・顧問スロットが機能しないことを保証
- [x] 冒険者支援室（新設）とスカウトの紐付け（→ 03 §7.3）2026年9月実装・テスト済み
- [x] クエスト適性ボーナス（→ 03 §4.2.1）2026年9月実装・テスト済み
- [x] 装備システム（4枠・職業制限・即時購入）（→ 03 §4.2.2）2026年9月実装・テスト済み
  - [x] Item/装備スロット/カタログの新設
  - [x] 個人CP・最大HPへの装備効果反映
  - [ ] AdventurerVisualDataとの連携（防具=上下同時、アクセサリ2は見た目反映なし）
        ※`AdventurerVisualData`自体が本リポジトリに存在しないため未着手（→ 03 §4.2.2・§11）
- [x] セーブ/ロードシステム（→ 03 §12）2026年9月実装・テスト済み
  - [x] SaveDataルートモデル・変換ロジック（相性Dictionary等の変換含む）
  - [x] 自動保存（週次決算後）・手動保存（UIボタン）
  - [x] ロード処理（起動時・失敗時のフォールバック）
- [x] 氏名ジェネレーター（和名・洋名、→ 03 §2.4）2026年9月実装・テスト済み
  - [x] マスターデータ修正（カタカナ・和名表記に統一）
  - [x] Gender（性別）フィールド追加（立ち絵連携は次フェーズ、今回はモデルのみ）
- [x] パーティー編成の永続化（→ 03 §4.0.2）2026年9月実装・テスト済み
  - [x] Party永続化（Id・Name・MemberIds、同時1パーティー所属制約）
  - [x] 派遣フロー変更（パーティー選択・出撃可能人数での自動出撃・一時的入れ替え）
  - [x] 編成画面での相性（険悪ペア）表示
  - [x] SaveDataへのSavedParties追加（v1.18作成時の漏れを解消）
- [x] 期限撤廃・Aランクフラグ（→ 03 §1.2・§8.2）2026年9月実装・テスト済み
- [x] クエスト期限延長・長期遠征クエストの解禁条件（→ 03 §4.0）2026年9月実装・テスト済み
- [x] 自動スキップ機能（→ 03 §1.3）2026年9月実装・テスト済み
  - [x] WeekResult・WeeklySettlementResultによる週次イベント検知
        （手動「次週へ」・自動スキップ共通のWeekProcessingSystemに集約）
  - [x] AutoSkipService（停止条件9種・上限1000週。指示書の8種に加え、
        敗北（破産）発生を停止条件として追加）
  - [x] UI（自動スキップボタン・結果サマリー表示）
- [x] UI改善：前衛/後衛の指定をパーティー編成画面に一本化、個人操作系ボタンの
      選択必須化（→ 03 §4.0.2・§4.2・§9）2026年9月実装・テスト済み
  - [x] メインダッシュボード個別詳細パネルの配置切り替えボタンを廃止し、
        パーティー編成画面（メンバー一覧・未編成一覧の両方から選択可）に移設
  - [x] 冒険者一覧を明示的な選択制に変更（先頭を暗黙に選択済み扱いにする
        フォールバックを廃止し、未選択時は引退・装備・昇給・ボーナス支給の
        4ボタンを無効化）
- [x] 中央ペインのタブ化（→03 §9）2026年9月実装・テスト済み
- [x] 初期メンバー4名のポートレート表示（→ 03 §2.1）2026年9月実装・テスト済み
  - [x] Adventurer.PortraitId追加（将来のモンタージュ方式への布石。未設定時はシルエット）
- [x] パーティー編成の深化（項目63〜66、→ 03 §4.2.3）2026年9月実装・テスト済み
  - [x] クエスト種別ごとの統一点数計算式（討伐/探索/護衛を対象ステータス・重み違いに統一。
        探索・護衛は3区分＋軽量HP消費・致死判定なし。旧クエスト適性ボーナスは廃止）
  - [x] 新特性2種（田舎育ち・知識人）と種別限定の個人特性ボーナス（QuestTypeScoreBonus）
  - [x] ペア特性シナジー（符号あり・種別限定・負の合計のみ隊長LDRで緩和）
  - [x] 探索・護衛のランダムイベント3種（強敵遭遇・宝物庫の発見・深追い）
  - [x] 週報表示（索敵/判定区分の日本語ラベル化・イベント結果の文章化・戦利品ボーナスの明示）
- [x] **女性限定ギルドの世界観設定を構造で保証（→ 03 §2.1・§11）2026年9月実装・テスト済み**
  - [x] `Models.Gender` から `Male` を削除し `Female` のみに（バランス値の書き換えでは
        男性が復活しない構造へ）
  - [x] `NameGenerator` から男性名プールと性別抽選（`RollGenderAndCulture`）を撤去し、
        `RollCulture`（文化圏のみ抽選）に変更
  - [x] `recruitment.csv` から `MaleGenderChancePercent` を削除
- [x] **JobClassを7職に拡張（→ 03 §2.1・§4.2・§4.2.2）2026年9月実装・テスト済み**
  - [x] Knight（騎士）・Thief（盗賊）・Scholar（学者）の3職を新設
  - [x] 成長重み（growth_job_weights.csv）を全7職業で合計9に統一
  - [x] 配置（前衛/後衛）の自動決定と配置補正倍率を7職業分に拡張
        （騎士＝重戦士型、学者＝魔導士型、盗賊＝斥候型）
  - [x] 装備の職業制限（大剣・魔導士の杖・重装鎧・ローブ）を7職業対応に拡張
  - [x] 採用試験の職業抽選を7職業に同期
- [x] **大迷宮（ダンジョン攻略）システムと8年稼働ライフサイクルの実装
      （→ 03 §3・§4.5・§9）2026年9月実装・テスト済み**
  - [x] `FloorBoss`・`IntelTier`：解析率を25/50/75/100%の4段階で開示
  - [x] 調査任務（`ScoutingResolver`）：隠密（AGI+DEX＋隊長LDR）・解析（INT）の
        2段構え判定。HP下限1・致死判定なしの低リスク経路
  - [x] 階層ボス討伐（`DungeonResolver`）：ギミック対策のOR判定（職業/ステータス/
        アイテム）、未対策の被ダメージ倍率累積、即死級未対策時のHP全損、
        完全解析時の与ダメージ+20%ボーナス
  - [x] `DungeonExpeditionSystem`：出撃の派遣・週次解決オーケストレーション
        （同時出撃枠を通常クエストと共有）
  - [x] UI：中央ペイン「大迷宮」タブ（`DungeonPanel.cs`）。ボス情報の段階的開示・
        出撃部隊選択・調査/討伐の出撃フロー・対策充足状況の警告表示・出撃予定一覧
  - [x] 週報ログ：調査任務の解析率上昇・討伐の決戦ステップ再生（昇格試験と同形式）
  - [x] 8年稼働・26歳満期引退モデルへの改訂は上記「8年稼働・26歳満期引退モデルへの
        全面改訂」を参照
- [x] **大迷宮の5フィールド拡張・走破力進軍・フィールドセレクターUI
      （→ 03 §4.5.1・§4.5.3・§4.5.6）2026年9月実装・テスト済み**
  - [x] 単一ダンジョン（ボス一本道）モデルを廃し、全5フィールド（各100階層・
        5階ごとに階層ボス計20体）へ拡張（`DungeonField`・`SampleData.CreateDefaultFields`）
  - [x] フィールド開放条件：10Fボス撃破で次フィールド開放（森→洞窟→廃墟→峡谷）、
        Order1〜4の20Fボス全撃破で最終フィールド（深淵）を開放
        （`DungeonExpeditionSystem.ApplyFieldProgression`）
  - [x] 走破力による道中進軍（`DungeonTraversalResolver`）：AGI+DEX＋隊長LDRの
        走破力で複数階層を一気に進軍（電撃/迅速/通常/苦戦の4区分）、一度倒した
        ボス階層は素通り、未撃破ボス階層のみ足止め（ストッパー）
  - [x] UIフィールドセレクター（OptionButton）：開放済みの複数フィールドを
        自由に選択して調査・討伐の派遣指示ができるよう`DungeonPanel`を拡張
- [x] **第3の任務「探索（採取）」と素材インベントリ（→ 03 §4.5.5）2026年9月実装・テスト済み**
  - [x] `GatheringResolver`：AGI/DEX/隊長LDR＋盗賊(Thief)・斥候(Ranger)ボーナスに
        応じた採取スコアで、フィールド固有素材と換金ゴールドを獲得
  - [x] `GameState.Materials`（素材Id→個数）の新設・セーブ/ロード対応
  - [x] `DungeonExpeditionSystem.TryDispatchGathering`：ボスを介さない常時出撃可能な
        派遣経路。HP下限1・致死判定なし
  - [x] UI：採取出撃ボタン・素材在庫ラベルを`DungeonPanel`に追加
  - [x] **候補Aとして実装確定（2026年9月、→ 03 §0.4）：** 素材を`materials.csv`
        （→ `Balance.MaterialBalance`）へCSV外部化。現時点は森（forest）フィールドの
        3種（月光草・霊樹の枝・変異胞子）のみ定義。抽選方式もフィールド固定％から
        素材ごとのMinFloor到達判定＋等確率抽選へ変更
- [x] **アルベールの研究室（素材投資システム）（→ 03 §4.6）2026年9月実装・テスト済み**
  - [x] `ResearchDefinition`/`ResearchBalance`（research.csv）/`ResearchSystem`：
        4種の恒久バフ（生体蛍光試薬・特殊保存嚢・軽量踏破靴・薬草湿布の調合）を
        素材＋ゴールドの即時投資で解禁
  - [x] `GameState.CompletedResearchIds`のセーブ/ロード対応（既存セーブとの
        互換性はJSONデフォルト値補完に依拠）
  - [x] `ScoutingResolver`/`GatheringResolver`/`DungeonTraversalResolver`/
        `RestRecoverySystem`への研究バフ参照追加
  - [x] UI：中央ペイン「研究室（Lab）」タブ新設（`ResearchPanel.cs`）。所持金・
        素材在庫一覧、研究カード（充足状況・実行ボタン・完了バッジ）
  - [x] **候補Aとして実装確定（2026年9月、→ 03 §0.4）：** research.csvのレシピを
        新素材Idへ全面差し替え（医務室特製霊薬→薬草湿布の調合へIdごと差し替え）。
        IntelRateBonusの適用式を直接倍率から(1+EffectValue)倍の加算率方式へ統一
- [x] **採取任務・素材インベントリ・研究室（候補A）：残る4フィールドの完全実装
      （→ 03 §4.5.1・§4.5.5・§4.6）2026年9月実装・テスト済み**
  - [x] `materials.csv`へ洞窟・廃墟・峡谷・深淵の8素材（各フィールド低層種＋希少種）を追加。
        全5フィールドで採取任務が成立するようになった
  - [x] 各フィールドの節目ボス（10F・20F）に、フィールド固有の希少素材（黒鉱石・
        ルーン石・紅玉鉱石・深淵の結晶）を確定ドロップとして設定（`SampleData.CreateFieldBosses`。
        forestは既存どおり全10体で3種を巡回）
  - [x] `research.csv`へ上位研究4種（硬化軟膏の調合・古代戦術録の解読・耐熱踏破法・
        魂魄安定の霊香）を追加し、研究室に計8種が並ぶように拡張
  - [x] `ResearchEffectType.SurvivalThresholdBonus`を新設し`DungeonResolver`に接続
        （致死判定を持たないダンジョン戦闘の設計上、HP消費率からの直接減算として実現）
  - [x] `Balance.ResearchBalance.GetTotalEffectValue`を新設し、RestRecoverySystem・
        ScoutingResolver・DungeonTraversalResolver・GatheringResolver・DungeonResolverを
        同種研究の合算加算へ改修（以前は研究Idを1つだけ固定でチェックしており、
        同種研究が複数存在すると2つ目以降が反映されない不具合があった）
- [x] **大迷宮の「毎回1Fリセット・複数週潜行型」への刷新（→ 03 §0.6・§4.5.3）
      2026年9月実装・テスト済み**
  - [x] `ExpeditionStatus`（Advancing／AwaitingBossDecision／EngagingBoss／Retreating）を新設し、
        `ActiveDungeonMission`に現在階層・扉前のボス・経過週数・道中の拾得物を追加（セーブ/ロード対応）
  - [x] 調査度連動の電撃走破（走破倍率1.0〜3.0倍、完全解析区間は被ダメージ0.3倍）を
        `DungeonTraversalResolver`に実装（dungeon_traversal.csvに係数を追加）
  - [x] ボス前ストッパー：扉前で進軍を止めて指令待ち。自動スキップの停止条件に追加
        （`WeekResult.BossDoorReached`）。指令待ちのまま週を越すと扉前で偵察
  - [x] `TryEngageBoss`（挑む：ポーチ代金を指令時に徴収、次週決戦）・`TryRetreat`
        （撤退：即時帰還し拾得物を格納）。帰還で出撃解除、次回は1階層から
  - [x] UI：潜行ボタン・扉前の指令エリア（⚔️挑む／🏃撤退）・潜行中一覧の状態表示・週報ログ
  - [x] 解決後に生還者を待機中へ戻す処理の堅牢化（例外時の帰還・取り残されたIsDispatchedの解除）と、
        出撃可能部隊が無い場合のUIフォールバック表示
  - [ ] 実機での通しプレイ確認（扉前の指令フロー・複数週潜行中のセーブ/ロード）
- [x] **迷宮調査コマンドの復活と4段階護衛判定（2026年9月実装・テスト済み）**
  - [x] `scouting.csv`に護衛判定のキー（GuardRatio_*・GuardEffect_IntelMultiplier_*・
        GuardEffect_HpLossPercent_*・BaseRequiredGuardPower）を追加し`ScoutingBalance`で読み込み。
        隠密成否によるHP消費（Stealth/DiscoveredHpLossPct*）は護衛判定に置き換えて廃止
  - [x] `ScoutingResolver`：`GuardTier`（余裕／十分／充足／不足）を新設。護衛力＝隊員中で最大の
        max(STR,VIT,INT)、要求護衛値＝BaseRequiredGuardPower×ボス階層÷10。解析率上昇量に段階倍率
        （不足は0＝潰走）、HP消費は最大HP×段階ごとの%（HP下限1、致死・除籍判定には非接続）
  - [x] `DungeonMissionType.Survey`・`DungeonExpeditionSystem.TryDispatchSurvey`：攻略対象ボスを
        1週で調査して帰還する独立任務（完全解析済みのボスには出撃不可、解析率は上限1.0）
  - [x] UI：「🔍 迷宮調査に出撃」ボタンを「大迷宮へ潜行」「探索出撃」と並べて再配置。
        部隊選択時に護衛評価をプレビュー（不足は赤字＋危険のツールチップ、出撃は可能）。
        週報ログに護衛段階ごとの状況文（残党を完封／被弾／潰走）を出力
- [x] **潜行の未踏破損耗と調査度ブーストの整理（→ 03 §0.7・§4.5.3）2026年9月実装・テスト済み**
  - [x] `dungeon.csv`に`UnexploredHpLossPctMin/Max`（30〜50%）を追加し`DungeonBalance`で読み込み
  - [x] `DungeonTraversalResolver`：未踏破階層（進軍前の最高到達階層より深い階層）を含む進軍は
        重損耗30〜50%を基礎にする（`TraversalResult.EnteredUnexplored`）。既踏のみの進軍は従来の
        進軍ランク別の消費率
  - [x] 調査度ブーストは既存の「進む階層数を最大3倍（1.0＋解析率×2.0）・完全解析区間の被ダメージ
        0.3倍」を正本として維持し、未踏破の重損耗にも同じ軽減を適用（走破力スコアへの倍率掛けは、
        進軍ランクの上限で頭打ちになるため採用しない）
  - [x] 仕様書03 §4.5.3を同期：隠密失敗によるHP消費の記述を撤去し、迷宮調査（Survey）・
        4段階護衛判定・未踏破の重損耗を正式仕様として記載
- [x] **旧通常クエストの完全撤去と大迷宮への完全一本化（→ 03 §0.8・§4）2026年9月実装・テスト済み**
  - [x] `Quest`系モデル・`QuestResolver`・`QuestDispatchSystem`・`QuestBoardSystem`・`QuestScoreCalculator`・
        `SecuritySystem`・`SuccessRateCalculator`・`PairSynergyCalculator`・`EmergencySupportSystem`・
        昇格試験（`GuildProgressionSystem`）と関連テストを削除。`quest*.csv`（5種）・`success_rate.csv`・
        `pair_synergy.csv`・`emergency.csv`、progression.csvの昇格試験キーも削除
  - [x] `GameState`/`SaveData`から`AvailableQuests`・`ActiveDispatches`・昇格試験フラグを削除
        （旧セーブは未知項目として無視され読み込める。互換テストあり）
  - [x] 同時出撃枠の判定を`DungeonExpeditionSystem.CanDispatch`へ移設。`WeekProcessingSystem`の
        コンストラクタから旧クエスト系の引数を削除
  - [x] 階層ボスの部隊火力を`DungeonPowerCalculator`（dungeon.csv `BossPowerWeight_*`）へ同じ数値のまま移設
  - [x] 累積功績（`TotalContributionScore`）を大迷宮へ再配線：進軍1階層ごと・ボス撃破（ボス階層分）・
        採取成功（dungeon.csv `Contribution*`、`DungeonExpeditionSystem.AwardContribution`）
  - [x] 相性「険悪」の満足度ペナルティの判定対象を、大迷宮へ出撃中の部隊へ移設
- [ ] 旧クエスト撤去で効果を失った仕組みの扱いを決める（大迷宮への再配線 or 仕様からの削除）
  - [x] 出撃経験による成長（経路1）：大迷宮の任務へ再配線済み（→ 下記「出撃経験による成長の大迷宮への再配線」）
  - [x] 勝利・功績の満足度ボーナス（§5.1）：大迷宮の任務成果へ再配線済み（→ 下記「任務成果による満足度変動の大迷宮への再配線」）
  - [x] 参謀ボーナス（AdvisorSystem、§7.2）：大迷宮の迷宮調査（ボス解析率加算）および道中潜行（走破力加算）へ再配線済み（→ 下記「作戦参謀ボーナスの大迷宮への再配線」）
  - [x] 脅威度（ThreatLevel）の増減経路（§4.4）：仕様から削除する形で決着
        （→ 下記「脅威度・治安システムの残骸整理」）
  - [ ] 名声の自然減衰（「現ランク相当のクエスト達成」が発生しないため、ボス撃破以外で名声を保てない）
  - [x] 休眠中のコード・CSV（環境ギミック`GimmickEvaluator`・`gimmick.csv`）の整理
        （→ 下記「旧ギミック評価器の撤去・出場機会ペナルティの全盛期整合」）。
        ※`security.csv`も撤去済み（→ 下記「脅威度・治安システムの残骸整理」）
  - [x] 残る休眠データの整理：`EnvironmentTag`と携行アイテムの相殺5種は撤去済み
        （→ Phase 5 の②）。特性「田舎育ち」の効果のみ未整理として残る

> **旧仕様の棚卸し（2026年9月、→ 03 §0.8〜§0.13）：** 上記の決着を、以下の5コミットで
> 一巡させた。詳細は各完了項目と仕様書の対応する改訂セクションを参照。
>
> | 内容 | 仕様書 |
> |---|---|
> | 旧通常クエストの完全撤去・大迷宮への一本化（成長・満足度・功績・参謀・討伐スコアの再配線を含む） | §0.8・§0.9 |
> | 脅威度（治安）システムの完全撤去・月次助成金のランク連動満額支給への純化 | §0.10 |
> | 年齢帯を実働3区分（新鋭/成長/全盛）へ純化 | §0.11 |
> | 旧ギミック評価器の一掃・出場機会ペナルティの全盛期（23〜26歳）整合 | §0.12 |
> | EnvironmentTag・旧相殺消耗品の撤去、消耗品カタログの大迷宮4種への純化 | §0.13 |
>
> いずれも旧セーブは「撤去した項目を未知/読み取り専用として無視する」形で読み込めることを
> テストで固定している（→ `SaveLoadTests`）。

- [x] **脅威度・治安システムの残骸整理（→ 03 §0.10・§4.4・§8.1・§8.3）2026年9月実装・テスト済み**
  - [x] `GameState.ThreatLevel`・`SaveData.ThreatLevel`を完全削除。旧セーブに残る
        `ThreatLevel`は未知項目として無視され、そのままロードできることをテストで担保
        （`SaveLoadTests.Load_RestoresOldSave_ContainingRemovedThreatLevelField`）
  - [x] 月次助成金の「脅威度75%超で50%カット」分岐を撤廃し、ギルド格付け連動の
        満額支給へ純化（`SubsidySystem`。`economy.csv`の`SubsidyThreatCutMultiplier`も削除）
  - [x] `SecurityBalance`・`docs/04_バランス表/security.csv`を削除。脅威度と無関係な
        `BankruptcyConsecutiveWeeksThreshold`（破産判定）のみ`economy.csv`／`EconomyBalance`へ移設
  - [x] 自動スキップ停止条件から`WeekResult.ThreatThresholdNewlyCrossed`を撤去（停止条件は9種→8種）
  - [x] UI（`main_dashboard.tscn`の`ThreatLabel`ノード・`MainDashboard`の脅威度ログ）を撤去

- [x] **年齢帯列挙型（`AgeBand.cs`）の整理＝到達不能年齢帯の撤去・実働3区分への純化
      （→ 03 §0.11・§3.0）2026年9月実装・テスト済み**
  - [x] 8年稼働モデル（18歳加入・26歳満期引退）で到達不能な円熟期（`MaturePeriod`）・
        限界期（`LimitPeriod`）を`AgeBand`から削除し、`Young`／`Growing`／`Peak`の3区分へ再定義
  - [x] 境界を〜18／19〜22／23〜へ再設定（`Adventurer.AgeBand`）。範囲外は近い側の帯へ
        丸める（18歳以下→新鋭期、27歳以上の旧データ・異常値→全盛期へクランプ）
  - [x] `aging.csv`の`GrowthProbability_MaturePeriod`／`_LimitPeriod`を削除し、
        新鋭期`GrowthProbability_Young`（0.35＝加入初年度を優遇）を新設。
        `GrowthBalance.GetBaseProbability`の`switch`も3分岐へ縮小
  - [x] 旧セーブに残る`AgeBand`（文字列・数値いずれも）が無視され、年齢から算出し直される
        ことをテストで担保（`SaveLoadTests.Load_RestoresOldSave_ContainingRemovedAgeBandValue`）
  - [x] UI（`AdventurerPanel.AgeBandLabel`）を新鋭期／成長期／全盛期の3区分へ同期

- [x] **旧ギミック評価器の撤去・出場機会ペナルティの全盛期整合（→ 03 §0.12・§5.1）
      2026年9月実装・テスト済み**
  - [x] 出場機会ペナルティの対象年齢を旧・全盛期22〜27歳から新・全盛期23〜26歳へ整合
        （`SatisfactionSystem.IsPeakAgeForOuting`）。22歳（成長期）は育成猶予として対象外
  - [x] 対象年齢をコード直書きから`satisfaction.csv`へ外部化
        （`OpportunityPenaltyMinAge`=23／`OpportunityPenaltyMaxAge`=26、`SatisfactionBalance`経由）
  - [x] 旧・環境ギミックの評価器一式（`GimmickEvaluator`・`GimmickBalance`・
        `GimmickMitigationLevel`・`gimmick.csv`・`GimmickEvaluatorTests`）を削除。
        本番コードからの参照は無く挙動への影響なし。ビルド出力の残存CSVも掃除
  - [x] `EnvironmentTag`と携行アイテムの相殺5種は、この時点ではアイテム定義側から参照が残るため据え置き
        （その後、Phase 5 の②で撤去。→ 03 §0.13）

- [x] **出撃経験による成長の大迷宮への再配線（→ 03 §3.1〜3.4 経路1）2026年9月実装・テスト済み**
  - [x] `dungeon.csv`に`GrowthRolls_Traversal/BossVictory/Survey/Gathering`（2/5/2/2回）・
        `GrowthBaseChancePercent`（35%）を追加し`DungeonBalance`で読み込み
  - [x] `GrowthSystem.ApplyExpeditionGrowth`：任務ごとの対象能力（潜行＝STR/VIT/AGI/DEX、ボス撃破＝全7能力、
        迷宮調査＝INT/DEX/LDR/AGI、採取＝AGI/DEX/VIT）から等確率で選び、成功で+1（PA上限でクランプ）。
        生存者のみ対象（強制除籍者・HP0は対象外）
  - [x] `DungeonExpeditionSystem`の各解決（1階層以上進軍した週・ボス撃破・迷宮調査の帰還・採取の帰還）へ配線。
        ボス討伐で撤退・全滅した場合は成長なし。成長結果は`DungeonMissionResolution.GrowthEvents`で週報に表示
- [x] **任務成果による満足度変動の大迷宮への再配線（→ 03 §5.1「勝利・功績」）2026年9月実装・テスト済み**
  - [x] `satisfaction.csv`に`Satisfaction_BossVictory`（+10）・`BossDefeat`（-5）・`SurveyAbundant`（+2）・
        `SurveySufficient`（+1）・`SurveyDeficient`（-5）・`TraversalSuccess`（+1）・`GatheringSuccess`（+1）を追加し
        `SatisfactionBalance`で読み込み（旧クエスト用の`VictoryBonus`は置き換えて削除）
  - [x] `SatisfactionSystem.ApplyExpeditionSatisfaction`：生存者のみ（強制除籍者・HP0は対象外）、0〜100にクランプ
  - [x] `DungeonExpeditionSystem`の各解決（出撃成長の呼び出し箇所と同列）へ配線。ボス戦の強制除籍時は、
        生存者に仲間ロストの余波（-30）と撃破／撤退の変動が加算で重なることをテストで確認
- [x] **作戦参謀ボーナス（Advisor System）の大迷宮への再配線（→ 03 §7.2）2026年9月実装・テスト済み**
  - [x] `compatibility_advisor.csv` に `Advisor_SurveyIntelBonusCoeff`（0.002）および `Advisor_TraversalPowerBonusCoeff`（0.2）を追加し `AdvisorBalance` で読み込み
  - [x] `AdvisorSystem.GetAdvisorSurveyIntelBonus`：引退時7能力平均×係数で迷宮調査の解析率加算率を算出。`ScoutingResolver` にて研究ボーナスと合算
  - [x] `AdvisorSystem.GetAdvisorTraversalPowerBonus`：引退時7能力平均×係数で道中潜行の走破力加算値を算出。`DungeonTraversalResolver.CalculateTraversalScore` にて研究ボーナスと並列加算
  - [x] 週報結果（`ScoutingResult`・`TraversalResult`）に支援参謀名とボーナス値を記録し、出撃前UIプレビューとも式を完全統一

## Phase 4：仕上げ

- [x] バランス値を xlsx → CSV へ外部化し、Core で読み込む（→ 03 §10.1・05 §3）2026年9月実装・テスト済み
  - [x] docs/04_バランス表/ のCSV14ファイルを正本化（旧xlsxは実コードとの乖離により廃止）
  - [x] Balance/配下12クラスのconstをCSV読み込みへ置き換え
  - [x] QuestResolver・AgingSystem・RecruitmentSystem・RestRecoverySystem・
        GameStateへの直書き値をBalance側へ集約（05 §3の方針違反を解消）
  - [x] CSV欠損・不正時は例外（フォールバックなし）
  - [x] フォローアップ：項目58時点で対象外だったTraitCatalog（特性の効果量）・
        ItemCatalog（装備の価格・効果量）もtrait.csv・equipment.csvを新設して
        CSV化（→ TraitBalance・EquipmentBalance）2026年9月実装・テスト済み
- [x] セーブ / ロード（オートセーブ＋手動保存）（→ 03 §12。上記Phase 3の項目として実装済み。
      「手動スロット」は単一スロットのみのため実装せず、post-MVP扱い→ 03 §11）
- [x] **冒険者管理パネル（AdventurerPanel）のUI刷新（→ 03 §9）2026年9月実装・テスト済み**
  - [x] 冒険者一覧（AdventurerList）の未選択状態厳守と防御的ガード（Null参照防止、誤操作防止）
  - [x] 7大能力値（STR/AGI/VIT/MND/DEX/INT/LDR）の実効値とPA（潜在能力上限）の併記表示
  - [x] 8年稼働モデル（18〜26歳）タイムライン、施設配属状況・装備・状態の可視化
- [x] **部隊編成パネル（PartyFormationPanel）のUI刷新とレイアウト比率調整（→ 03 §4.0.2・§9）2026年9月実装・テスト済み**
  - [x] 解放出撃枠（UnlockedSquadSlots）連動の最大4部隊セレクター（未開放枠Disabledロック）
  - [x] 出撃中ガード（IsDispatched部隊の編集・解除ロック、「出撃中・編成変更不可」バッジ）
  - [x] メンバー4枠カードのスリム化（最小高さ148px）、職業に応じた【前衛】／【後衛】自動判定バッジの可視化、解除ボタン
  - [x] 待機候補一覧スクロール枠の拡張（最小高さ360px、垂直拡張）、即時「配属 ＋」ボタン
  - [x] 部隊総合力サマリー（走破力・隠密・解析・HP健全度・相性険悪ペア警告）のリアルタイムプレビュー
  - [x] scenes/PartyFormationPanel.tscn とルート party_formation_panel.tscn の完全同期
- [x] **作戦週報（右ペイン）440px拡張と顧問管理ボタンの人事画面移設（→ 03 §9）2026年9月実装・テスト済み**
  - [x] 作戦週報（RightPanel）の横幅を440px（custom_minimum_size = Vector2(440, 0)）に固定拡張し、日本語20〜24文字の不自然な折り返しを解消
  - [x] ResultLogコンテナをVertical ExpandFill化し、縦全域を作戦ログ表示領域として最大化
  - [x] 右ペイン下部の「顧問管理」ボタンを冒険者管理パネル（AdventurerPanel）のヘッダー領域へ移設
  - [x] AdventurerPanelからMainDashboardへのAdvisorRequestedイベント配線と安全なガード処理
- [x] **メインダッシュボードのメニューナビゲーション遷移型刷新（→ 03 §9）2026年9月実装・テスト済み**
  - [x] 常設ヘッダー直下に水平メニューナビゲーションバー（NavigationBar）を新設（大迷宮・人事・編成・研究・施設・顧問室の6ボタン）
  - [x] CenterPanelのタブヘッダー非表示化（tabs_visible=false）とスタックコンテナ化（AdventurerTab入れ子解消・直接子ノード展開）
  - [x] 「大迷宮」表示時のみ右ペイン（作戦週報440px）を表示し、内政4画面では非表示にして中央ペインを全幅（約1900px）へ拡張
  - [x] アクティブボタンのトグル押し込み＆ゴールドハイライト表示、および「顧問室」ボタンからのAdvisorPopupモーダル起動
- [x] **部隊編成画面の縦肥大化によるUIスタック（ヘッダー・ナビ画面外押し出し）根本解決（→ 03 §9）2026年9月実装・テスト済み**
  - [x] MainDashboardを単一VBoxContainer（アンカーFull Rect）直下の4層構造（%HeaderPanel 40px / %NavigationBar 45px / MainContentArea ExpandFill / %FooterPanel 55px）へ整流化
  - [x] PartyFormationPanelをScrollContainer（%PartyFormationScroll）でラップし、中央ペインのコンテンツ縦あふれによる常設枠の画面外押し出しを構造的に遮断
  - [x] PartyFormationPanelのCandidateScroll最小高さを260pxに最適化し、パネル全体を約615px（900px以内）にコンパクト化（scenes版・ルート直下版の完全同期）
  - [x] 画面遷移テスト：部隊編成画面表示時も全メニューボタンが常時可視（IsVisibleInTree=true）を維持し、全画面への双方向遷移を保証
- [x] **大迷宮パネル（DungeonPanel）の3ゾーンUIレイアウト刷新・ブラッシュアップ（→ 03 §4.5.6）2026年9月実装・テスト済み**
  - [x] Zone A（進軍モニター）：電撃進軍速度倍率バッジ（`⚡ 進軍速度倍率：×{speed:F1} Speed`）、潜行中部隊の現在地ピン（📍）、最高到達階層・目標ボス・全体制覇数・拾得リソースカウンターの整流化
  - [x] Zone B（ボス警戒・ポーチ）：ボスHPバー・数値表示（`%BossHpBar` / `%BossHpLabel`、基本情報開示後にHP/%表示、未解析時は？？？マスク）、4大ギミック（猛毒・重装甲・飛行・即死級）の2×2 GridContainer配置（未解析マスク、危険度★、対策条件、部隊＋ポーチ連動の✔充足/✖未対策/✔安全判定）、携行ポーチ独立カード化（重複選択の自動相殺リセット、所持金連動警告）
  - [x] Zone C（部隊状況・コマンド）：出撃部隊セレクター、部隊4名ミニカード（ポートレート、職業、前後衛バッジ、HPバー・数値）、総合見立てプレビュー（潜行走破力・迷宮調査護衛評価・探索採取スコア）
  - [x] 状況連動型コマンド群の排他制御：待機時（`🏃 大迷宮へ潜行（進軍）` / `🔍 迷宮調査に出撃（解析）` / `🌿 探索出撃（素材採取）`）、進軍中（`🏃 緊急帰還指示`）、扉前決断（`⚔️ ボス討伐に挑む` / `🏃 撤退・帰還する`）
  - [x] `scenes/DungeonPanel.tscn` とルート直下 `dungeon_panel.tscn` の完全同期（git diff 0件維持）
- [ ] 左ペイン（人事・内政タブ）と施設投資 UI の作り込み
- [ ] UI 美装・アイコン・レイアウトのリサイズ耐性仕上げ
- [x] 氏名ジェネレータ（→ 03 §2.4。上記Phase 3の項目として実装済み。姓・二つ名はpost-MVP→ 03 §11）
- [ ] 音・演出（特にロスト時の演出）
- [ ] エンドレスモード移行処理
- [ ] 通しプレイでのバランス調整（数値を回して繰り返す）

## Phase 5：大迷宮・研究室まわりの残タスク（post-MVP、→ 03 §4.2.2・§11）

> 大迷宮5フィールド拡張・第3の任務（採取）・アルベールの研究室（→ Phase 3）の
> 実装が一段落したところで見えてきた、隣接する未着手項目。優先度は未確定。

- [x] **パーティ携行アイテムポーチ（→ 03 §4.2.2「入手」・§4.5.4・§11）
      2026年9月実装・テスト済み**：出撃前ポーチ（最大2枠）の対策アイテムUIとCore層接続完了。
  - [x] 対策アイテムを4ギミック全種に拡充（猛毒＝解毒薬、重装甲＝溶解液〈新設〉、
        飛行＝捕縛網〈新設〉、即死級＝護符）
  - [x] `DungeonExpeditionSystem.TryDispatch`：ボス討伐出撃時のみ携行アイテムの
        合計代金を即時徴収（不足時は出撃失敗）、`TryCancel`で全額返金
  - [x] UI：ポーチスロット2枠（OptionButton）を`DungeonPanel`に新設。選択変更で
        ギミック対策充足（✔／✖）がリアルタイムに再計算され、代金不足時は
        出撃ボタンを無効化
- [x] **ボス撃破報酬演出（→ 03 §4.5.1・§4.5.4・§4.5.6）2026年9月実装・テスト済み**：
      撃破時の所持金・名声・素材ドロップ、および新フィールド開放・出撃枠拡張の
      演出ログを実装。
  - [x] `FloorBoss.RewardMaterialId`/`RewardMaterialCount`を新設し、撃破報酬に
        素材確定ドロップを追加（`ApplyFieldProgression`が`GameState.Materials`へ加算）
  - [x] `DungeonMissionResolution.FieldNewlyUnlocked`を新設し、その週に新規開放された
        フィールドを検知できるように
  - [x] 週報ログへ🏆撃破・💰報奨・👑名声・📦素材・🗺新フィールド開放・📡出撃枠拡張の
        演出行を追加（`MainDashboard.LogDungeonMission`）
- [x] **大迷宮パネル（DungeonPanel）UI刷新（2026年9月実装・テスト済み）**：進軍モニター（Zone A）、ボス警戒・ポーチ（Zone B）、部隊状況・コマンド（Zone C）の3ゾーン構造コンテナ設計、Unique Name参照、同一アイテム重複選択防止、メンバー4名ミニカード表示、状況連動型コマンド排他制御を実装。
### 未着手（優先度順。→ 03 §11「直近の優先タスク」と対応）

- [ ] **①遺産・形見装備**（→ 03 §11・「魂魄定着の儀」構想の一部）：強制除籍・満期引退した
      冒険者の装備を「形見」として保持し、次世代への継承やコレクション要素にする。
      8年稼働モデルで世代交代が必ず起きるため、世代をまたぐ蓄積の受け皿として最優先
- [x] **②旧相殺消耗品と`EnvironmentTag`の整理**（→ 03 §0.13・§4.2.3・§4.5.4）
      **2026年9月実装・テスト済み**：単純削除で決着し、消耗品を大迷宮ボスギミック対策
      4種へ純化した
  - [x] `EnvironmentTag`と`ConsumableItem.CounterTag`を削除。ギミックの体系は大迷宮の
        `BossGimmickType` 1系統のみになった
  - [x] カタログを解毒薬・溶解液・捕縛網・身代わりの護符の4種へ純化（聖水・松明・登攀具・
        煙幕弾・高品質傷薬・携帯糧食と、対応する`consumables.csv`のキー・
        `ConsumableEffectType`の効果種別3つを削除）
  - [x] アイテム→ギミックの対応をUIのハードコード表から`ConsumableItem.TargetGimmick`へ移設。
        `DungeonPanel`はカタログ全件を選択肢に並べるだけになり、二重管理が解消
  - [x] `QuestEventOutcome`→`SurveyOutcome`へ改名（→ 03 §4.5.3）。孤立コードと見込んでいたが
        迷宮調査の解析結果として現役だったため、削除ではなく実態に合わせた改名で決着
  - [x] `ConsumableCatalogTests`を新設：4種のロード・ギミックとの1対1対応・撤去済みIDが
        nullを返す（古いセーブの`Party.ConsumableItemIds`を安全に読める）ことを検証
- [ ] **③最深部（100F）制覇によるクリア判定・エンディング**（→ 03 §11・§8.2）：
      現状の勝利条件は「最終討伐クエスト」として構想のみで、大迷宮側に終端が無い。
      全5フィールド制覇または最深部到達をゲーム終了条件として接続するかを決める
- [ ] 血統・配合システム（→ 03 §11「魂魄融和の秘薬」構想）：相性最大のペアから
      新人を誕生させる、女性限定ギルドの世界観に沿った途中採用の経路

## 継続的にやること

- [ ] 決定が変わったら `03` `05` を更新し、理由をコミットに残す
- [ ] 遊んでみて出た「暫定値の調整」は `docs/04_バランス表/` 配下のCSVを直接編集して行う
      （→ 03 §10.1。ビルド不要、実行ファイルの隣のCSVを差し替えれば反映される）
